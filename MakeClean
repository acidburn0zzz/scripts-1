#!/bin/zsh

for i ($@) do
	case $i in
	--force)  rmeach Makefile              ;;
	--noexec) rmexec                       ;;
	*)        echo 'Unknown param'; exit   ;;
	esac
done

rmeach CMakeFiles
rmeach *.cmake
rmeach CMakeCache.txt
rmeach *.a

exit
